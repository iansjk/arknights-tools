import { Operator } from "../../scripts/output-types";

interface Props {
  operator: Operator | null;
}

const GoalSelect: React.VFC<Props> = () => {
  return <div>GoalSelect</div>;
};
export default GoalSelect;
